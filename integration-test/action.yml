{
  "name": "Hello World",
  "inputs": {
    "token": {
      "description": "GitHub Token",
      "required": true
    }
  },
  "description": "Greet someone and record the time",
  "runs": {
    "using": "node20",
    "main": "dist/actions.mjs"
  }
}
